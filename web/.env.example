# 前端运行时配置样例（复制为 .env.local 后按需填写）
#
# 注意：VITE_ 前缀的变量会打包进产物，请勿放置敏感凭证。

# ─── 可观测性 ───────────────────────────────────────────────
# 错误与性能监控 DSN（兼容 Sentry 协议）。留空时监控层为 no-op，不影响业务。
# 自托管可使用 GlitchTip：https://glitchtip.com
VITE_SENTRY_DSN=

# 应用版本号，用作监控 release 标识。构建时由 CI 注入，本地可省略。
# VITE_APP_VERSION=0.8.2

# 环境标识：development / staging / production。默认取 vite MODE。
# VITE_APP_ENV=production

# 生产 canonical URL。留空时不会输出 canonical，避免误写未知域名。
# VITE_CANONICAL_URL=https://mail.example.com/
